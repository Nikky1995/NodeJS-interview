
console.log('we are inside multiply and executing');

export let multiply = (a, b) => {
    console.log('multiply', a*b)
    
}
//module.exports = {multiply}; common js module structure of exporting the functions and variables