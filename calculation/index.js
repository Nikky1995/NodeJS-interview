const {sum} = require('./sum'); // Common JS module structure of importing the functions and variables
const {multiply} = require('./multiply'); // Common JS module structure of importing the functions and variables

module.exports = {sum, multiply}; // Common JS module structure of exporting the functions and variables